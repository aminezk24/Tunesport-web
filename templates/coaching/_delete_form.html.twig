<form method="post" action="{{ path('app_coaching_delete', {'idcoa': coaching.idcoa}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ coaching.idcoa) }}">
    <button class="btn">Delete</button>
</form>
