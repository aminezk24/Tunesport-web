{% extends 'base.html.twig' %}

{% block title %}Edit Coaching{% endblock %}

{% block body %}
    <h1>Edit Coach</h1>

    {{ include('coaching/_form.html.twig', {'button_label': 'Update Coach'}) }}
    {{ include('coaching/_delete_form.html.twig') }}
    <a href="{{ path('app_coaching_index') }}">
        <button class="btn btn-primary btn-sm text-center" type="button" style="margin-right: 40px;margin-bottom: 90px;background: var(--bs-cyan);">Back To List</button>
    </a>


{% endblock %}
