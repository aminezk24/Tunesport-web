{% extends 'base.html.twig' %}

{% block title %}Edit Commentaires
{% endblock %}

{% block body %}
	<div class='container pt-5 pb-5'>
		<h1>Edit Commentaires</h1>

		{{ include('commentaires/_form.html.twig', {'button_label': 'Update'}) }}
		<button class='btn btn-info'>
			<a href="{{ path('app_commentaires_index') }}" style='color:white'>back to list</a>
		</button>
		{{ include('commentaires/_delete_form.html.twig') }}
	</div>
{% endblock %}
